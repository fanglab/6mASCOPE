from unittest import SkipTest
import logging
import json
import os.path as op

from pysiv2.custom.base import TestReportStatistics

log = logging.getLogger(__name__)
logging.basicConfig(level=logging.DEBUG)


class TestRawDataRpt(TestReportStatistics): # aka filter_stats_xml
    """
    Tests for dataset report generation.
    """
    REPORT_ID = "raw_data_report"
    TEST_ID = REPORT_ID
    METRIC_IDS = [
        "nbases",
        "nreads",
        "read_length",
        "read_n50",
        "subread_length",
        "subread_n50",
        "insert_length",
        "insert_n50",
    ]


class TestAdapterReport(TestReportStatistics):
    REPORT_ID = "adapter_xml_report"
    TEST_ID = REPORT_ID
    METRIC_IDS = [
        "adapter_dimers",
        "short_inserts",
        "local_base_rate_median"
    ]
