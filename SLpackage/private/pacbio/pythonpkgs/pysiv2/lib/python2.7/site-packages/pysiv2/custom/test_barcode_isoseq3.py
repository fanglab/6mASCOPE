from unittest import SkipTest
import logging
import json
import os.path as op

from pysiv2.custom.base import TestReportStatistics

log = logging.getLogger(__name__)
logging.basicConfig(level=logging.DEBUG)


class TestBarcodeIsoSeq3(TestReportStatistics):
    """
    Supplementary barcode report tests for Iso-Seq 3 pipeline (separate from
    non-IsoSeq metrics in test_barcoding).
    """
    REPORT_ID = "barcode_isoseq3"
    TEST_ID = REPORT_ID
    METRIC_IDS = [
        "n_reads_total",
        "num_reads_fl",
        "num_reads_flnc",
        "num_reads_flnc_polya"
    ]
